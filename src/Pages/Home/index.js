/* eslint-disable react/prop-types */
import { Box, Heading } from "@chakra-ui/react";
const Home = ({ title }) => {
  return (
    <Box>
      <Heading>{title}</Heading>
    </Box>
  );
};

export default Home;
